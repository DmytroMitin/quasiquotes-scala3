package quasiquotes.scalameta

/** Test-only symmetric umbrella candidate for the explicit Scalameta route. */
object Quasiquotes:
  export ScalametaQuasiPattern.{qq, tqq}
  export ScalametaQuasiquotes.{qr, tqr}
