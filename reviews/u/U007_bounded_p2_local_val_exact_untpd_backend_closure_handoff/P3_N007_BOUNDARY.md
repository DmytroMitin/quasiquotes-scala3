# P3 and N007 boundary

N007 landed before U007 launch and was controller-accepted during U007. It provides a neutral Scalameta projection for one bounded local identity method using existing `BlockStatement.LocalDef`.

U007 did not add method binder, parameter binder, recursion, owner, result-type, or local-def lowering. A corrupt `LocalDef` constructed term remains rejected by both richer source-free and generated-origin Block paths. The direct backend remains closed as well.

No N007 source was modified. N008 constructor projection is unrelated future neutral work.

```text
P3_BOUNDARY = PRESERVED
```
